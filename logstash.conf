input {
  beats {
    port => 5044
  }
}

filter {
  # aquí puedes parsear campos comunes si quieres
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "%{[@metadata][beat]}-%{+YYYY.MM.dd}"
  }
}
